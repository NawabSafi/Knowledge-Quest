<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SemesterProject"
        tools:targetApi="31">
        <activity
            android:name=".DataActivity"
            android:exported="false" />
        <activity
            android:name=".Forms.FormActivity"
            android:exported="false" />
        <activity
            android:name=".SignUp.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".UI.CalculatorActivity"
            android:exported="false" />
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login.LoginActivity"
            android:exported="false">

            <!-- Remove the LAUNCHER category for LogIn activity -->
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- Keep MainActivity as a launcher activity if needed -->
        </activity>
    </application>

</manifest>